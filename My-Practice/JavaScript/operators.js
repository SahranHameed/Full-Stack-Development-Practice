
// Logical AND operator
console.log(true && true); // true (if only all true - all the function is true)
console.log(true && false); // false (if one false - all the function is false)

// Logical OR Operator
console.log(true || true); // true  } (if only one is true - all the function is true)
console.log(false || true); // true } 

// Logical NOT operator
console.log(!true); // false } Working is oposit dieraction
console.log(!false); // true }



// If else Exercise //
var season = "winter";
    if(season == "spring")
    {
        console.log("Enjoy the blooming floweres.")
    }

    if(season == "summer")
    {
          console.log("Have fun in the Sun.")
    }

        if(season == "autumn")
    {
          console.log("Admire the colorful leaves.")
    }

    if(season == "winter")
    {
          console.log("Bundle up and stay warm.")
    }



// Odd number and Even number//
    var number = ("odd")
    if(number == "even")
    {
          console.log("The number is even")
    }
    else
        {
            console.log("The number is odd")
        }


// For Loop //
for(count=1; count<=10; count=count+1)
{
    console.log(count) // Print Numbers 1 to 10
    console.log("Sahran") // 10 Times Print Names
}


// For Loop //
for(count=1; count<=10; count=count+2)
{
    console.log(count) // Print Numbers 1 to 10, but Increment bt 2 (Odd Number)
 
}


for(count=10; count<=10; count=count+1)
{
    console.log(count) // Print Numbers 1 to 10, but Increment bt 2 (Odd Number)
 
}